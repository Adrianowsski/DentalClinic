<Window x:Class="DentalClinicWPF.Views.Room.AddRoomView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Add New Room" Height="300" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <StackPanel>
            <Label Content="Room Number:" />
            <TextBox Text="{Binding Room.RoomNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" />

            <Label Content="Available:" />
            <CheckBox IsChecked="{Binding Room.IsAvailable}" Margin="0,5" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="5" />
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="5" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>