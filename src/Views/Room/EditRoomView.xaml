<Window x:Class="DentalClinicWPF.Views.Room.EditRoomView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Edit Room" Height="300" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <StackPanel>
            <Label Content="Room Number:" />
            <TextBox Text="{Binding EditingRoom.RoomNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" />

            <Label Content="Available:" />
            <CheckBox IsChecked="{Binding EditingRoom.IsAvailable}" Margin="0,5" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="5" />
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="5" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Width="100" Margin="5" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>