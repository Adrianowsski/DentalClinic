<UserControl x:Class="DentalClinicWPF.Views.Invoice.InvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DentalClinicWPF.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Toolbar -->
            <RowDefinition Height="Auto" /> <!-- Sorting -->
            <RowDefinition Height="*" />    <!-- Invoice List -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox Width="300" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" 
                     Margin="0,0,10,0" 
                     ToolTip="Search by patient, dentist, treatment, or invoice ID" />
            <Button Content="Search" Command="{Binding SearchCommand}" Margin="0,0,10,0" Width="100" />
            <Button Content="Reload" Command="{Binding ReloadCommand}" Margin="0,0,10,0" Width="100" />
            <Button Content="Add Invoice" Command="{Binding AddInvoiceCommand}" Width="150" Margin="0,0,10,0" />
            <Button Content="Edit Invoice" Command="{Binding EditInvoiceCommand}" 
                    Width="150" Margin="0,0,10,0" 
                    IsEnabled="{Binding SelectedInvoice, Converter={StaticResource NullToBoolConverter}}" />
            <Button Content="View Details" Command="{Binding OpenInvoiceDetailCommand}" 
                    Width="150" Margin="0,0,10,0" 
                    IsEnabled="{Binding SelectedInvoice, Converter={StaticResource NullToBoolConverter}}" />
        </StackPanel>

        <!-- Sorting -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <ComboBox Width="150" ItemsSource="{Binding SortOptions}" 
                      SelectedItem="{Binding SelectedSortOption}" 
                      ToolTip="Select sorting criterion" 
                      Margin="0,0,10,0" />
            <Button Content="Sort" Command="{Binding SortCommand}" Width="100" />
        </StackPanel>

        <!-- Invoice List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Invoices}" 
                  AutoGenerateColumns="False" 
                  SelectedItem="{Binding SelectedInvoice}" 
                  IsReadOnly="True" 
                  SelectionMode="Single" 
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice ID" Binding="{Binding InvoiceID}" Width="Auto" />
                <DataGridTextColumn Header="Patient" Binding="{Binding Appointment.Patient.DisplayInfo}" Width="*" />
                <DataGridTextColumn Header="Dentist" Binding="{Binding Appointment.Dentist.DisplayInfo}" Width="*" />
                <DataGridTextColumn Header="Treatment" Binding="{Binding Appointment.Treatment.Name}" Width="*" />
                <DataGridTextColumn Header="Appointment Date" Binding="{Binding Appointment.AppointmentDate, StringFormat=yyyy-MM-dd}" Width="Auto" />
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="Auto" />
                <DataGridCheckBoxColumn Header="Paid" Binding="{Binding IsPaid}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
