<UserControl x:Class="DentalClinicWPF.Views.Invoice.InvoiceDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DentalClinicWPF.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    
    <UserControl.Resources>
        <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter" />
    </UserControl.Resources>
    
    <Grid Background="#FFFFFF">
        <Border BorderThickness="1" BorderBrush="#DDDDDD" CornerRadius="6" 
                Background="#FAFAFA" Padding="20" Margin="10">
            <StackPanel>
                <TextBlock Text="Invoice Details"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Margin="0,0,0,10"/>
    
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
    
                    <!-- Invoice ID -->
                    <TextBlock Text="Invoice ID:" FontWeight="Bold" Grid.Row="0" Grid.Column="0" 
                               Margin="0,0,0,5" />
                    <TextBlock Text="{Binding Invoice.InvoiceID}" Grid.Row="0" Grid.Column="1" 
                               Margin="0,0,0,5" />
    
                    <!-- Patient -->
                    <TextBlock Text="Patient:" FontWeight="Bold" Grid.Row="1" Grid.Column="0" 
                               Margin="0,5,0,5" />
                    <TextBlock Text="{Binding Invoice.Appointment.Patient.DisplayInfo}" Grid.Row="1" Grid.Column="1" 
                               Margin="0,5,0,5" />
    
                    <!-- Dentist -->
                    <TextBlock Text="Dentist:" FontWeight="Bold" Grid.Row="2" Grid.Column="0" 
                               Margin="0,5,0,5" />
                    <TextBlock Text="{Binding Invoice.Appointment.Dentist.DisplayInfo}" Grid.Row="2" Grid.Column="1"
                               Margin="0,5,0,5"/>
    
                    <!-- Treatment -->
                    <TextBlock Text="Treatment:" FontWeight="Bold" Grid.Row="3" Grid.Column="0" 
                               Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding Invoice.Appointment.Treatment.Name}" 
                               Grid.Row="3" Grid.Column="1" Margin="0,5,0,5"/>
    
                    <!-- Appointment Date -->
                    <TextBlock Text="Appointment Date:" FontWeight="Bold" Grid.Row="4" Grid.Column="0" 
                               Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding Invoice.Appointment.AppointmentDate, StringFormat={}{0:yyyy-MM-dd}}" 
                               Grid.Row="4" Grid.Column="1" Margin="0,5,0,5"/>
    
                    <!-- Total Amount -->
                    <TextBlock Text="Total Amount:" FontWeight="Bold" Grid.Row="5" Grid.Column="0" 
                               Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding Invoice.TotalAmount, StringFormat={}{0:C}}" 
                               Grid.Row="5" Grid.Column="1" Margin="0,5,0,5"/>
    
                    <!-- Paid -->
                    <TextBlock Text="Paid:" FontWeight="Bold" Grid.Row="6" Grid.Column="0" 
                               Margin="0,5,0,5"/>
                    <TextBlock Text="{Binding Invoice.IsPaid, Converter={StaticResource BoolToYesNoConverter}}" 
                               Grid.Row="6" Grid.Column="1" Margin="0,5,0,5"/>
                </Grid>
    
                <!-- Buttons: Preview, Print, and Export PDF -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
                    <Button Content="Preview"
                            Command="{Binding PreviewCommand}"
                            Margin="0,0,10,0"
                            Width="80"/>
    
                    <Button Content="Print"
                            Command="{Binding PrintCommand}"
                            Margin="0,0,10,0"
                            Width="80"/>
    
                    <Button Content="Export PDF"
                            Command="{Binding ExportPdfCommand}"
                            Width="100"/>
                </StackPanel>
    
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
